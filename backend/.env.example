# Self-hosted E2EE sync backend configuration
#
# Deploy your own Cloudflare Worker from this directory, then set the app's
# Worker URL to the deployed endpoint.

E2EE_WORKER_URL=https://your-worker.example.workers.dev
